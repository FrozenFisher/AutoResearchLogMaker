# 使用淘宝镜像源（解决 SSL 证书问题）
registry=https://registry.npmmirror.com

# Electron 镜像（通过环境变量设置）
# 在命令行中设置：set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/

# 如果仍然遇到 SSL 问题，可以临时禁用（仅用于开发环境）
# strict-ssl=false

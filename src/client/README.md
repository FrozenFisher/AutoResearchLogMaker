# AutoResearchLogMaker Client

AutoResearchLogMaker 桌面客户端，基于 Electron + Vue 3 + TypeScript 开发。

## 功能特性

- 📁 **文件管理**：按日期上传和管理文件（PDF/图片/Excel）
- 🔄 **工作流管理**：创建、启动和监控工作流执行
- 🛠️ **工具配置**：管理和配置自定义工具
- 📊 **结果查看**：查看研究总结和元数据

## 技术栈

- **前端框架**：Vue 3 + Composition API + TypeScript
- **UI组件库**：Element Plus
- **状态管理**：Pinia
- **路由**：Vue Router
- **HTTP客户端**：axios
- **构建工具**：Vite + Electron Builder

## 开发

### 安装依赖

```bash
cd src/client
npm install
```

### 启动开发服务器

```bash
# 同时启动 Vite 和 Electron
npm run electron:dev

# 或仅启动前端
npm run dev
```

### 构建

```bash
# 构建生产版本
npm run build

# 打包为可执行文件
npm run dist
```

## 配置

创建 `.env` 文件（参考 `.env.example`）：

```env
VITE_API_BASE=http://localhost:8000
```

## 注意事项

1. 确保服务器已启动（默认端口 8000）
2. 首次使用需要配置项目名称（默认为 `default_project`）
3. 上传文件大小限制为 50MB
